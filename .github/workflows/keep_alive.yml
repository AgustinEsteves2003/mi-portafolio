name: Mantener Vivo el Portfolio

on:
  schedule:
    # Se ejecuta cada 2 horas (puedes ajustarlo)
    - cron: '0 */2 * * *'
  workflow_dispatch: # Permite ejecutarlo manualmente si quieres

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping a la URL
        run: |
          curl -I https://mi-portafolio-966zuqcbrho9dogtrsksne.streamlit.app/
